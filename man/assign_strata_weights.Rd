% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/StrataAGB.R
\name{assign_strata_weights}
\alias{assign_strata_weights}
\title{Assign strata weights to circular plots}
\usage{
assign_strata_weights(plots, strata_weights = c(A = 0.22, B = 0.22, C = 0.68))
}
\arguments{
\item{plots}{A data frame containing plot data with at least a \code{stratum} column (character or factor).}

\item{strata_weights}{A named numeric vector where names are strata levels and values are weights.
Defaults to \code{c("A" = 0.22, "B" = 0.22, "C" = 0.68)}.}
}
\value{
A data frame with an additional \code{wt} column containing the assigned weights.
}
\description{
Assigns predefined strata weights to circular plots based on their strata classification.
This is an internal helper function used by \code{StrataAGB} and is not intended for direct use.
}
\examples{
  # Example: Assign weights to a simple plot data frame
  strata_plots <- data.frame(stratum = c("A", "B", "C"))
  weighted_plots <- assign_strata_weights(strata_plots)
  print(weighted_plots)
}
