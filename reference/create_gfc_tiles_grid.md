# Create a global grid of GFC tiles

Creates a global grid of 10x10 degree tiles covering the world, matching
the Hansen Global Forest Change product naming convention.

## Usage

``` r
create_gfc_tiles_grid()
```

## Value

An `sf` object representing the global grid of GFC tiles.
