# Detect filename patterns using machine learning approach

Detect filename patterns using machine learning approach

## Usage

``` r
detect_patterns_ml(filenames)
```

## Arguments

- filenames:

  Vector of filenames to analyze

## Value

List with pattern configuration or NULL if detection fails
