# Create circular plots

Create circular plots

## Usage

``` r
polyCirc(coords_poly, radius = 10)
```

## Arguments

- coords_poly:

  A data frame with POINT_X and POINT_Y columns.

- radius:

  Numeric, the radius of the circular plot in meters (default: 10).

## Value

An sf object representing the circular plot.
